# 태그로 연락처 관리하기

## 개요
- 연락처 또는 저장하지 않은 번호에 태그를 추가해 관리할 수 있음
- 총 2개의 화면이 있음. 연락처 화면, 통화기록 화면, 환경설정 화면이며, 통화기록 화면이 메인 화면임.
- 각각의 화면은 앱 하단 또는 상단에 화면 전환 버튼이 있어야 함.
- 태그는 문자열이며, 최대 10자까지 입력 가능함.
- 태그는 애플리케이션이 설치된 기기의 로컬에 저장되어야 함.

### 연락처 화면
- 연락처 화면은 기본적으로 다른 일반 연락처 화면과 동일함.
- 단, 저장된 이름뿐만 아니라, 태그로도 검색할 수 있어야 함.
- 연락처 목록은 지정된 이름, 첫 번째 태그 순으로 우선순위를 정해 목록에 표시.
- 표시된 이름 또는 태그의 첫 글자 초성으로 그룹화하여 나눠서 표기
- 각 연락처 목록에는 태그 추가하기, 전화하기, 문자보내기 버튼이 존재해야 함.
- 전화하기 버튼을 클릭 시 기본 dial 화면으로 전환함.
- 문자보내기 버튼을 클릭 시 기본 문자 보내기 화면으로 전환함.
- 각 연락처 목록은 클릭 시 해당 연락처의 기본 정보를 팝업으로 표시함.
- 연락처의 기본 정보는 아래와 같음.
  - 저장된 이름(또는 연락처)이 제일 위에 표시.
  - 태그 목록(순서 변경 및 직접 편집 가능)
  - 태그 목록의 마지막은 빈 칸으로 두고 직접 태그를 추가할 수 있음.

## 통화기록 화면
- 통화기록 화면은 저장된 연락처, 첫 번째 태그 순으로 우선순위를 정해 목록에 표시.
- 각 통화기록은 저장된 연락처가 없다면 연락처 저장, 태그 추가, 전화, 문자 버튼이 있음.
- 각 통화기록은 저장된 연락처가 있다면 연락처 저장 버튼은 표시하지 않음.
- 전화, 문자 버튼은 연락처 화면의 전화, 문자 버튼과 같은 역할
- 각 통화기록은 클릭하여 해당 연락처와의 통화기록을 팝업으로 표시함.
  - 통화 음성녹음과 문자 내용을 간략히 표시

## 환경설정 화면
- 연락처 화면, 통화기록 화면, 환경설정 화면의 배경화면을 각각 설정할 수 있음.
- 앱 전체의 폰트 크기를 변경할 수 있음.
- 폰트 색, 배경 색 등을 변경할 수 있음.
- 태그 입력 시 기기에 저장된 태그 중 일치하는 태그를 자동으로 보여주는 기능을 끄고 켤 수 있음.


## 코드 생성 시 지침
- 코드 생성 시 한국어로 주석 남겨야 함.
- flutter 애플리케이션의 디렉터리 구조와 변수 명명규칙 등의 규약을 따르도록 하여야 함.
- 코드 생성 시 추가한 부분이 있다면 아래 [추가사항](#추가사항)에 추가.

# 추가사항

- 문서에 나열된 통화기록, 연락처, 환경설정의 총 3개 화면을 Android용 Flutter 앱으로 구현함. 통화기록을 메인 화면으로 사용함.
- `lib/models`, `controllers`, `services`, `screens`, `widgets`, `utils`로 역할을 분리하고 한국어 UI 및 주석을 적용함.
- 태그는 최대 10개의 사용자 인식 문자까지 허용하며, 중복 검사·직접 편집·삭제·드래그 순서 변경·자동 추천을 지원함. 미등록 번호에도 직접 추가할 수 있음.
- 저장된 이름 → 첫 태그 → 번호 순서로 표시하며 이름·태그·번호·한글 초성 검색과 초성 그룹화를 지원함. 국내 번호의 국제 표기는 같은 저장 키로 정규화함.
- Android 기본 API를 플랫폼 채널로 연결하여 연락처·통화기록·SMS 조회와 기본 전화·문자·연락처 앱 실행을 지원함. 기능별 읽기 권한을 선택적으로 요청함.
- 통화기록은 100건씩 추가 조회하고 연락처는 번호별 인덱스로 조회하여 목록마다 기기 쿼리를 반복하지 않음. 문자 미리보기는 해당 번호의 최근 SMS 50건으로 제한함.
- 녹음은 사용자가 시스템 문서 선택기에서 선택한 오디오 파일을 번호에 연결하고 기본 재생 앱으로 확인함. 시스템 비공개 녹음의 자동 조회 및 MMS·RCS 조회는 지원 범위에 포함하지 않음.
- 태그·설정·녹음 연결 정보는 앱 전용 JSON에 원자적으로 저장하고 백업 및 기기 이전에서 제외함. 손상된 데이터 로딩에 실패하면 기존 파일을 덮어쓰지 않음.
- 화면별 배경 이미지, 앱 글자 배율·색상, 배경 색상, 태그 추천 사용 여부를 설정하고 로컬에 유지함. 이미지는 용량과 해상도를 제한하여 메모리 사용량을 줄임.
- 한국어 UI용 `flutter_localizations`, 사용자 인식 글자 수 계산용 `characters`, Android 검증용 SDK 패키지 `integration_test`를 사용함. 별도 상태 관리 및 기기 연동 라이브러리는 도입하지 않음.
- 단위·화면·Android 통합 테스트와 테스트 전용 에뮬레이터 준비 스크립트를 추가함. 실행 방법, 로컬 저장 정책, 플랫폼 및 배포 제약은 README.md에 기록함.
- 연락처·통화기록의 빈 목록 및 검색 결과 없음 안내를 공통 컴포넌트에서 가로 중앙에 정렬함.
- 권한 허용 안내 카드가 화면의 좌우 여백 안에서 사용 가능한 전체 너비를 채우도록 함.

